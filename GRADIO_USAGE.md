# 🧬 PyPDA Gradio Web界面使用指南

## 🚀 快速开始

### 方法1：一键启动（推荐）
```bash
# 确保在pypda目录下
python gradio_launcher.py
```

### 方法2：手动启动
```bash
# 安装依赖
pip install gradio>=4.0.0

# 启动Web界面
python gradio_launcher.py
```

启动后，浏览器会自动打开 http://localhost:7860

## 📋 功能介绍

### 1. 🎯 获取蛋白质序列
**用途**：批量获取指定基因的蛋白质序列和结构域信息

**使用方法**：
1. 点击"🎯 获取蛋白质序列"标签页
2. 在文本框中输入基因名称，用空格分隔（例如：`BRCA1 TP53 EGFR`）
3. 点击"获取序列"按钮
4. 等待处理完成后下载结果文件

**输出文件**：
- 每个基因的FASTA序列文件
- 结构域信息报告（domain_info.md）
- 打包的ZIP文件包含所有结果

**存储位置**：`temp/protein_sequences/genes_YYYYMMDD_HHMMSS/`

### 2. ✂️ 序列提取
**用途**：从FASTA文件中提取指定位置的子序列

**使用方法**：
1. 点击"✂️ 序列提取"标签页
2. 上传FASTA文件
3. 输入起始和结束位置（1-based索引）
4. 点击"提取序列"按钮
5. 下载提取的子序列

**存储位置**：`temp/extract_output/extract_YYYYMMDD_HHMMSS/`

### 3. 🧬 序列突变
**用途**：对蛋白质序列执行点突变

**使用方法**：
1. 点击"🧬 序列突变"标签页
2. 上传FASTA文件
3. 输入突变位置（用逗号分隔，如：`10,20,30`）
4. 输入对应的新氨基酸（用逗号分隔，如：`A,G,L`）
5. 点击"执行突变"按钮
6. 下载突变后的序列

**存储位置**：`temp/mutate_output/mutate_YYYYMMDD_HHMMSS/`

### 4. 🔄 序列比对
**用途**：对多个蛋白质序列进行两两比对

**使用方法**：
1. 点击"🔄 序列比对"标签页
2. 上传至少两个FASTA文件
3. 点击"开始比对"按钮
4. 查看比对结果和相似性信息

**存储位置**：`temp/align_output/align_YYYYMMDD_HHMMSS/`

### 5. 📊 UniProt数据
**用途**：获取指定蛋白质的详细UniProt信息

**使用方法**：
1. 点击"📊 UniProt数据"标签页
2. 输入蛋白质名称（如：`TP53`或`BRCA1`）
3. 点击"获取数据"按钮
4. 下载JSON格式的原始数据和Markdown格式的分析报告

**存储位置**：`temp/uniprot_reports/uniprot_YYYYMMDD_HHMMSS/`

### 6. 🏗️ PDB处理
**用途**：下载和处理PDB结构文件，支持与用户提供的小分子结构进行相似性计算

**使用方法**：
1. 点击"🏗️ PDB处理"标签页
2. 输入蛋白质名称（如：`BRCA1`）
3. （可选）输入小分子的SMILES字符串（如：`CC(=O)NC1=CC=C(C=C1)O`）用于结构相似性计算
4. 点击"处理PDB"按钮
5. 下载包含PDB文件和分析结果的ZIP包

**存储位置**：`temp/pdb_output/pdb_YYYYMMDD_HHMMSS/`

## 📁 文件说明

### 输入文件格式
- **FASTA文件**：标准的FASTA格式，扩展名可以是`.fasta`、`.fa`、`.txt`
- **基因名称**：使用标准基因符号，如`BRCA1`、`TP53`、`EGFR`等

### 输出文件说明
- **FASTA文件**：包含蛋白质序列的标准FASTA格式
- **JSON文件**：UniProt API返回的完整数据结构
- **Markdown文件**：易读的分析报告
- **ZIP文件**：包含所有相关结果的压缩包

## ⚠️ 注意事项

1. **网络连接**：部分功能需要访问UniProt和PDB数据库，请确保网络连接正常
2. **文件大小**：大文件处理可能需要较长时间，请耐心等待
3. **输入验证**：确保输入格式正确，基因名称和蛋白质名称使用标准符号
4. **结果保存**：所有结果都保存在临时目录中，请及时下载需要的文件
5. **错误处理**：如果操作失败，请查看界面上的错误信息

## 🛠️ 故障排除

### 常见问题

**Q: 启动失败怎么办？**
A: 检查Python环境，确保已安装所有依赖：
```bash
pip install -r requirements.txt
```

**Q: 基因序列获取失败？**
A: 检查基因名称是否正确，确保网络连接正常，可尝试单个基因

**Q: 文件上传失败？**
A: 检查文件格式是否正确，确保文件不是空的

**Q: 处理超时？**
A: 大型蛋白质或复杂分析可能需要更长时间，请耐心等待

### 查看日志
所有错误信息都会显示在Web界面上，也可以查看项目根目录下的`error.txt`文件获取详细日志。

## 📞 技术支持

如遇到问题，请：
1. 查看界面上的错误信息
2. 检查`error.txt`日志文件
3. 确认输入格式和参数正确
4. 确保网络连接正常

---

**PyPDA Gradio界面让蛋白质数据分析变得简单直观！** 🧬✨